// Export navigation items with translation keys.
// In Navbar.tsx, you can import this array and map over it to render links with translated labels.
export const navigation = [
  { labelKey: "home", path: "/" },
  { labelKey: "about", path: "/about" },
  { labelKey: "contact", path: "/contact" },
  // ...navigationItems
];

// Example usage in Navbar.tsx:
//
