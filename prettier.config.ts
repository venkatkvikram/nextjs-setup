import type { Options } from "prettier";

const config: Options = {
  semi: true,
  singleQuote: false,
  tabWidth: 2,
  trailingComma: "es5",
  bracketSpacing: true,
  arrowParens: "always",
  printWidth: 100,
};

export default config;
